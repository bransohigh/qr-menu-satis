# ─────────────────────────────────────────────────────────────────────────────
# QR Menü SaaS — Ortam Değişkenleri
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun.
# Hiçbir zaman gerçek .env dosyasını Git'e eklemeyin!
# ─────────────────────────────────────────────────────────────────────────────

# ── Genel ─────────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3000

# Uygulamanın herkese açık URL'si (QR kodları vb. için kullanılır)
# Lokal: http://localhost:3000  |  Canlı: https://alanadi.com
APP_URL=http://localhost:3000

# ── Veritabanı (Supabase) ────────────────────────────────────────────────────
# DATABASE_URL → runtime bağlantısı (Supabase pooler önerilir, port 6543)
#   ör: postgresql://postgres.PROJE_ID:SIFRE@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true
#
# DIRECT_URL → prisma migrate/seed için direkt bağlantı (port 5432)
#   ör: postgresql://postgres.PROJE_ID:SIFRE@aws-0-eu-central-1.pooler.supabase.com:5432/postgres
DATABASE_URL="postgresql://postgres:SIFRE@localhost:5432/qrmenu?schema=public"
DIRECT_URL="postgresql://postgres:SIFRE@localhost:5432/qrmenu?schema=public"

# ── Güvenlik ─────────────────────────────────────────────────────────────────
# En az 32 karakter: openssl rand -hex 32
JWT_SECRET=changeme_use_at_least_32_chars_long_random_string_here

# ── Cookie / Oturum ──────────────────────────────────────────────────────────
COOKIE_NAME=oturum

# ── Dosya Yükleme ────────────────────────────────────────────────────────────
UPLOAD_DIR=uploads
MAX_UPLOAD_MB=5

# ── Reverse Proxy (Hostinger/Nginx arkasında ise true yapın) ─────────────────
TRUST_PROXY=false

# ── Ödeme Sağlayıcı ──────────────────────────────────────────────────────────
# Seçenekler: fakepay | stripe | iyzico
PAYMENT_PROVIDER=fakepay

# Stripe (PAYMENT_PROVIDER=stripe ise gerekli)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Iyzico (PAYMENT_PROVIDER=iyzico ise gerekli)
IYZICO_API_KEY=
IYZICO_SECRET_KEY=
