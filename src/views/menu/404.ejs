<!DOCTYPE html>
<html lang="en">
<head><%- include('../partials/menu-head') %></head>
<body class="bg-slate-50 min-h-screen flex flex-col items-center justify-center px-4">
  <div class="text-center max-w-md">
    <div class="text-8xl mb-6">🍽</div>
    <h1 class="text-4xl font-extrabold text-slate-800 mb-2">Not Found</h1>
    <p class="text-slate-500 mb-8"><%= title || 'The page you are looking for does not exist.' %></p>
    <% if (typeof menu !== 'undefined' && menu) { %>
      <a href="<%= baseUrl %>" class="inline-block bg-brand-600 hover:bg-brand-700 text-white font-semibold px-6 py-3 rounded-2xl transition-smooth">
        Back to Menu
      </a>
    <% } else { %>
      <a href="/themes" class="inline-block bg-brand-600 hover:bg-brand-700 text-white font-semibold px-6 py-3 rounded-2xl transition-smooth">
        Browse Menus
      </a>
    <% } %>
  </div>
</body>
</html>
