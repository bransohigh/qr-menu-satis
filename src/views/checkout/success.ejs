<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ödeme Başarılı — QR Menu</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="min-h-screen bg-gradient-to-br from-green-50 to-emerald-50 flex items-center justify-center p-4">

  <div class="w-full max-w-md text-center">
    <!-- Success icon -->
    <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6 shadow-inner">
      <svg class="w-10 h-10 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
        <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
      </svg>
    </div>

    <h1 class="text-3xl font-bold text-gray-900 mb-2">Ödeme Başarılı!</h1>
    <p class="text-gray-500 mb-8">
      <% if (purchase && purchase.theme) { %>
        <strong class="text-gray-700"><%= purchase.theme.name %></strong> teması aktive edildi.
        Menünüz hemen kullanıma hazır.
      <% } else { %>
        Satın alımınız başarıyla tamamlandı. Menünüz aktive edildi.
      <% } %>
    </p>

    <div class="bg-white rounded-2xl shadow p-6 mb-6 text-left space-y-3">
      <% if (purchase) { %>
      <div class="flex justify-between text-sm">
        <span class="text-gray-500">İşlem ID</span>
        <span class="text-gray-800 font-mono text-xs"><%= purchase.id.slice(0, 8) %>...</span>
      </div>
      <div class="flex justify-between text-sm">
        <span class="text-gray-500">Tutar</span>
        <span class="font-semibold text-gray-900">
          <%= purchase.currency === 'USD' ? '$' : '₺' %><%= Number(purchase.amount).toFixed(2) %>
        </span>
      </div>
      <div class="flex justify-between text-sm">
        <span class="text-gray-500">Durum</span>
        <span class="bg-green-100 text-green-700 text-xs font-semibold px-2 py-0.5 rounded-full">Ödendi</span>
      </div>
      <% } %>
    </div>

    <a href="/admin"
      class="inline-block w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-3 px-6 rounded-xl transition-colors mb-3">
      Menüyü Düzenlemeye Başla →
    </a>
    <a href="/themes"
      class="inline-block w-full border border-gray-200 text-gray-500 hover:text-gray-700 hover:bg-gray-50 font-medium py-2.5 px-6 rounded-xl transition-colors text-sm">
      Tüm Temalara Dön
    </a>
  </div>

</body>
</html>
