<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>FakePay — Ödeme Simülasyonu</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="min-h-screen bg-gray-50 flex items-center justify-center p-4">

  <div class="w-full max-w-md">

    <!-- FakePay badge -->
    <div class="text-center mb-6">
      <span class="inline-flex items-center gap-2 bg-yellow-100 border border-yellow-300 text-yellow-800 text-xs font-semibold px-3 py-1.5 rounded-full">
        <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13 16h-1v-4h-1m1-4h.01M12 20a8 8 0 100-16 8 8 0 000 16z" />
        </svg>
        GELIŞTIRICI MODU — Sahte Ödeme
      </span>
    </div>

    <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
      <!-- Header -->
      <div class="bg-gradient-to-r from-indigo-600 to-violet-600 p-6 text-white">
        <div class="text-xs font-semibold uppercase tracking-wider opacity-70 mb-1">FakePay Checkout</div>
        <div class="text-3xl font-bold">
          <%= purchase.currency === 'USD' ? '$' : '₺' %><%= Number(purchase.amount).toFixed(2) %>
        </div>
        <div class="text-sm opacity-80 mt-1"><%= theme.name %> teması</div>
      </div>

      <!-- Order summary -->
      <div class="p-6 border-b border-gray-100">
        <h3 class="text-sm font-semibold text-gray-500 uppercase tracking-wide mb-3">Sipariş Özeti</h3>
        <div class="flex justify-between text-sm">
          <span class="text-gray-700"><%= theme.name %> — Ömür Boyu Lisans</span>
          <span class="font-semibold text-gray-900">
            <%= purchase.currency === 'USD' ? '$' : '₺' %><%= Number(purchase.amount).toFixed(2) %>
          </span>
        </div>
        <div class="flex justify-between text-sm mt-2 pt-2 border-t border-gray-100">
          <span class="font-semibold text-gray-900">Toplam</span>
          <span class="font-bold text-indigo-600">
            <%= purchase.currency === 'USD' ? '$' : '₺' %><%= Number(purchase.amount).toFixed(2) %>
          </span>
        </div>
      </div>

      <!-- Fake card form (purely cosmetic) -->
      <div class="p-6 space-y-4">
        <div>
          <label class="block text-xs font-semibold text-gray-500 mb-1">Kart Numarası</label>
          <input type="text" value="4242 4242 4242 4242" disabled
            class="w-full border border-gray-200 rounded-lg px-3 py-2.5 text-sm text-gray-400 bg-gray-50 cursor-not-allowed" />
        </div>
        <div class="grid grid-cols-2 gap-3">
          <div>
            <label class="block text-xs font-semibold text-gray-500 mb-1">Son Kullanma Tarihi</label>
            <input type="text" value="12 / 28" disabled
              class="w-full border border-gray-200 rounded-lg px-3 py-2.5 text-sm text-gray-400 bg-gray-50 cursor-not-allowed" />
          </div>
          <div>
            <label class="block text-xs font-semibold text-gray-500 mb-1">CVC</label>
            <input type="text" value="123" disabled
              class="w-full border border-gray-200 rounded-lg px-3 py-2.5 text-sm text-gray-400 bg-gray-50 cursor-not-allowed" />
          </div>
        </div>
        <p class="text-xs text-gray-400 text-center">
          ⚠️ Bu alan bir ödeme simülasyonudur. Gerçek kart bilgisi girilmez.
        </p>
      </div>

      <!-- Actions -->
      <div class="px-6 pb-6 space-y-3">
        <form method="POST" action="/pay/fake/confirm">
          <input type="hidden" name="purchaseId" value="<%= purchase.id %>" />
          <input type="hidden" name="action" value="pay" />
          <button type="submit"
            class="w-full bg-indigo-600 hover:bg-indigo-700 active:bg-indigo-800 text-white font-semibold py-3 rounded-xl transition-colors">
            Ödemeyi Tamamla — <%= purchase.currency === 'USD' ? '$' : '₺' %><%= Number(purchase.amount).toFixed(2) %>
          </button>
        </form>
        <form method="POST" action="/pay/fake/confirm">
          <input type="hidden" name="purchaseId" value="<%= purchase.id %>" />
          <input type="hidden" name="action" value="cancel" />
          <button type="submit"
            class="w-full border border-gray-200 text-gray-500 hover:text-gray-700 hover:bg-gray-50 font-medium py-2.5 rounded-xl transition-colors text-sm">
            İptal Et
          </button>
        </form>
      </div>
    </div>

    <p class="text-center text-xs text-gray-400 mt-4">
      Güvenli ödeme · Şifreli bağlantı · 30 gün iade garantisi
    </p>
  </div>

</body>
</html>
